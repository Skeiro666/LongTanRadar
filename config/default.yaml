# 寻龙尺 — 涨停龙头股研究与交易时机识别系统（代码包名仍为 ashare）
product:
  name: 寻龙尺
  name_en: XunLongChi
  tagline: 涨停龙头 · 连板研究 · 买点识别
  description: 从涨停池识别连板龙头，持续跟踪 Focus 名单，在合适买点发出 BUY_READY 提醒；默认纸面模拟，不自动实盘。

leader:
  enabled: true
  research_only: true

trading:
  mode: backtest
  execute_at: open
  lot_size: 100

universe:
  mode: leader
  # symbols 仅在网络/龙头池失败时的兜底，日常全市场扫描，不会只用这几只
  symbols:
    - 601288.SH
    - 601398.SH
    - 601988.SH
    - 601328.SH
    - 600016.SH
    - 000001.SZ
    - 601166.SH
    - 601818.SH
    - 600919.SH
    - 002142.SZ
  screen:
    min_price: 2.0
    # 纸面本金 3000：1 手最多约 30 元/股；再留手续费余量 → 28
    max_price: 28.0
    min_amount: 50000000
    exclude_st: true
    exclude_bj: true
    exclude_kcb: true   # 科创板单价高，小资金难买 1 手
    exclude_cyb: true   # 创业板（科技成长）波动大、不少高价股
    max_pct_chg: 19.5
    max_daily_gain: null
    rank_by: leader
    max_candidates: 50

pool:
  use_limit_up: true
  use_strong: true
  use_profit_gap: true
  use_tech_leader: true
  max_candidates: 40
  min_amount: 80000000
  min_price: 3.0
  max_price: 28.0
  exclude_st: true
  exclude_bj: true
  exclude_kcb: true
  exclude_cyb: true
  tech_min_pct_chg: 3.0
  tech_top: 25
  min_profit_yoy_pct: 50.0

research:
  enabled: true

factors:
  weights:
    rs_20: 0.18
    breakout: 0.14
    vol_confirm: 0.10
    trend: 0.12
    board: 0.18
    profit_gap: 0.16
    event: 0.08
    liquidity: 0.04

paper:
  initial_balance: 3000
  state_file: data/paper_state.json
  reset_on_start: false

costs:
  commission_rate: 0.00025
  min_commission: 5.0
  stamp_tax_rate: 0.0005
  transfer_fee_rate: 0.00001
  slippage_bps: 5

ai:
  enabled: true
  # 聚合商：一个 Key + 一个 URL，圆桌只换 model 名
  # 硅基流动 https://api.siliconflow.cn/v1
  # 阿里云百炼 https://dashscope.aliyuncs.com/compatible-mode/v1
  # 默认示例为硅基流动；.env 可整段覆盖为百炼等
  provider: siliconflow
  base_url: https://api.siliconflow.cn/v1
  model: deepseek-ai/DeepSeek-V4-Flash
  api_key_env: AI_API_KEY
  timeout_sec: 180
  temperature: 0.2
  max_tokens: 4096
  use_openai_sdk: true
  review_backtest: true
  roundtable: true
  roundtable_mode: sampled  # benchmark | sampled | scheduled | disabled
  roundtable_sample_every: 10
  roundtable_max_per_day: 1
  trade_review: false
  cache_dir: data/cache/ai_decisions
  committee:
    mode: multi_model
    risk_sees_others: true
    # 角色默认只写 model；base_url/key 继承全局（.env 可改模型名）
    roles:
      - id: dragon
        name: 龙头研究员
        model: Qwen/Qwen3.5-122B-A10B
      - id: event
        name: 事件/业绩研究员
        model: deepseek-ai/DeepSeek-V4-Flash
      - id: risk
        name: 风控官
        model: Pro/moonshotai/Kimi-K2.6
      - id: chair
        name: 投委会主席
        model: deepseek-ai/DeepSeek-V4-Flash
        temperature: 0.15
  cost_tracking:
    enabled: true
    log_path: data/ai/usage.jsonl
    usd_per_1m:
      default_input_per_1m: 0.5
      default_output_per_1m: 1.5
      models:
        deepseek-ai/DeepSeek-V4-Flash:
          input_per_1m: 0.14
          output_per_1m: 0.28
        Qwen/Qwen3.5-122B-A10B:
          input_per_1m: 0.86
          output_per_1m: 3.44
        moonshotai/Kimi-K3:
          input_per_1m: 0.60
          output_per_1m: 2.40

strategy:
  name: leader
  picks_style: leader
  scoring:
    w_ml_z: 0.55
    w_mr_z: 0.45
    require_ml_nonneg: true
    conflict_penalty: 1.2
    agree_weight: 1.0
    mr_only_weight_scale: 0.25
    ml_only_weight_scale: 0.5
    min_ml_pred: 0.006
    zero_mr_on_breakdown: true
    skip_breakdown_without_ml: true
  anti_chase:
    max_daily_ret: 0.02
    max_mom20: 0.12
    max_ma_gap20: 0.06
    ban_dump_ret: -0.04
  fast: 10
  slow: 30
  max_positions: 3
  rebalance_threshold: 0.05
  lookback_mom: 20
  lookback_vol: 20
  lookback_value: 60
  top_n: 3
  rebalance: week_end
  w_mom: 0.1
  w_vol: 0.35
  w_value: 0.55

ml:
  label_horizon: 5
  top_n: 3
  valid_ratio: 0.2
  n_estimators: 120
  train_start: "2021-01-01"
  train_end: "2024-06-30"
  models_dir: data/models
  grid_num_leaves: [15, 31]
  grid_learning_rate: [0.05, 0.1]
  grid_min_data_in_leaf: [20, 50]

risk:
  max_name_weight: 0.55
  max_gross_weight: 0.98
  max_drawdown: 0.30
  max_daily_loss: 0.15

backtest:
  start: "2022-01-01"
  end: "2024-12-31"

data:
  provider: akshare
  use_sample_if_empty: true
  cache_dir: data/cache
  start: "2021-01-01"
  refresh_lag_days: 5
  screen_hist_days: 420
  history_expand_batch: 900

monitor:
  log_dir: logs
  log_level: INFO

storage:
  database_url: postgresql+psycopg://postgres:postgres@127.0.0.1:5432/ashare
  redis_url: redis://127.0.0.1:6379/0

broker:
  mode: paper
  qmt_userdata_path: ""
  qmt_account_id: ""

agent:
  autostart: false
  interval_sec: 1800
  paper_only: true

optimizer:
  auto_apply: false  # V5: proposals go to experiments; never direct prod by default
